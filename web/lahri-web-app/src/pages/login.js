import { Container } from "@mui/material";
import React from "react";

class Login extends React.Component {
    render() {
      return (
        <Container fixed>
            <div>
                <h1>Log in with Google, bitches!!!</h1>
            </div>
        </Container>
      );
    }
  }

export default Login;